// See https://aka.ms/new-console-template for more information
using System.Security.Cryptography;
using System.Text;

Console.WriteLine("Enter 123 phrase:");
MD5 md5 = MD5.Create();
string s = Console.ReadLine();
byte[] hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}


s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}
s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}


s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}
s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

s = Console.ReadLine();
hash = md5.ComputeHash(Encoding.ASCII.GetBytes(s));

foreach(byte b in hash)
{
    Console.Write(b.ToString("X2"));
}

